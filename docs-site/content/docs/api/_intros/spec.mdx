This single endpoint serves the OpenAPI document that describes this
entire API — the same machine-readable contract used to generate the
reference pages you're reading, the Rust (`hm`) CLI client, and the
dashboard's typed client.

Fetch it to drive your own codegen or to import Harmont into an API
client. It needs no authentication.
