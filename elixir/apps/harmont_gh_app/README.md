# HarmontGhApp

The Harmont GitHub App: webhook handling (HMAC/JWT), the installation-token
cache, and the Check Run reporter that reflects build status back to GitHub.

## Installation

If [available in Hex](https://hex.pm/docs/publish), the package can be installed
by adding `harmont_gh_app` to your list of dependencies in `mix.exs`:

```elixir
def deps do
  [
    {:harmont_gh_app, "~> 0.1.0"}
  ]
end
```

Documentation can be generated with [ExDoc](https://github.com/elixir-lang/ex_doc)
and published on [HexDocs](https://hexdocs.pm). Once published, the docs can
be found at <https://hexdocs.pm/harmont_gh_app>.

